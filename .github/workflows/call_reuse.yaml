name: Access output from other workflow

on: [push]

jobs:
  call-reusable-job-ouput-workflow:
    uses: ./.github/workflows/reusable_job_output.yml

  print-output:
    runs-on: ubuntu-latest
    needs: call-reusable-job-ouput-workflow
    if: ${{ needs.call-reusable-job-ouput-workflow.outputs.firstword == 'hell' }} 
    steps:
      - run: |
          echo ${{ needs.call-reusable-job-ouput-workflow.outputs.firstword }}
          
